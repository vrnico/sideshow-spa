# Copy this file to .env.local and fill in your real key.
# .env.local is gitignored, so the key never gets pushed to GitHub.
# On the live site, set the same variable in Vercel:
#   Vercel → your project → Settings → Environment Variables
FREAKSCENE_API_KEY=
